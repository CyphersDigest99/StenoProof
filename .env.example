# Option 1: Use your local Claude Code CLI (requires Claude Max subscription)
# This routes requests through your Claude Code terminal installation
USE_CLAUDE_CODE=true

# Option 2: Use Anthropic API directly (requires API key)
# Get your API key from https://console.anthropic.com/
# ANTHROPIC_API_KEY=sk-ant-your-key-here

# If neither is set, the app runs in demo mode with sample data
